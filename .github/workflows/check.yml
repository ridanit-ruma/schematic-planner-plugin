name: Check

on:
  push:
  pull_request:

jobs:
  structural:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - run: sh scripts/sync-harnesses.sh
      - run: sh scripts/check.sh
